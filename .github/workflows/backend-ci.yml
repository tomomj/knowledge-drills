name: Backend CI

on:
  pull_request:
    paths:
      - "backend/**"
      - "agent/**"
      - ".github/workflows/backend-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: backend-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up uv
        # v8 系はメジャーエイリアスタグが無いためパッチまで固定する
        uses: astral-sh/setup-uv@v8.3.0
        with:
          enable-cache: true
          cache-dependency-glob: backend/uv.lock

      - name: Install dependencies
        run: uv sync --frozen

      - name: Lint
        run: uv run --native-tls --frozen ruff check .

      - name: Typecheck
        run: uv run --native-tls --frozen mypy .

      - name: Test
        run: uv run --native-tls --frozen pytest

  agent:
    name: Agent
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: agent

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up uv
        # v8 系はメジャーエイリアスタグが無いためパッチまで固定する
        uses: astral-sh/setup-uv@v8.3.0
        with:
          enable-cache: true
          cache-dependency-glob: agent/uv.lock

      - name: Install dependencies
        run: uv sync --frozen

      - name: Lint
        run: uv run --native-tls --frozen ruff check .

      - name: Typecheck
        run: uv run --native-tls --frozen mypy .

      - name: Test
        run: uv run --native-tls --frozen pytest
