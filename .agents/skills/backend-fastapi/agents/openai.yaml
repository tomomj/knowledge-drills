interface:
  display_name: "Backend FastAPI"
  short_description: "Knowledge Drills の FastAPI / Firestore / ADK backend 規約"
  default_prompt: "$backend-fastapi を使って Knowledge Drills の FastAPI backend を実装またはレビューする。"
