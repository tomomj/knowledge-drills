# Implementation Plan

- [x] 1. diff 行数集計の純粋関数を追加する
  - unified diff 文字列から追加行数と削除行数を集計する(`+++` / `---` のファイルヘッダ行は除外、空入力は 0/0)
  - React に依存しない純粋関数として実装し、追加のみ・削除のみ・空文字列・ヘッダ行のみのケースを単体テストで検証する
  - 新規単体テストが green になっていること
  - _Requirements: 5.1_
  - _Boundary: lib/diffStats_

- [x] 2. 分析タイムラインを実行状態つきの実行ログにする
- [x] 2.1 (P) ステップの実行状態表現を追加する
  - 実行中ステップに動きのあるインジケーター、完了ステップにチェックアイコン+summary+根拠、未開始ステップは控えめなステップ名のみ、失敗・スキップは既存 chip 表現を維持する
  - ポーリング更新で running → completed に遷移した際に summary と根拠が追加表示される
  - prefers-reduced-motion 有効時はアニメーションを無効化し内容を即時表示する(CSS media query)
  - タイムラインのアクセシブルな名前(aria-label)を維持する
  - 各状態の表示分岐を component テストで検証し green になっていること
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.7, 7.3, 7.5_
  - _Boundary: AnalysisTimeline, App.css のタイムライン状態クラス_

- [x] 2.2 ステップ間の所要時間を表示する
  - 直前の完了ステップと自ステップの完了時刻が共に存在する場合のみ差分秒を表示し、先頭ステップや欠損時は表示しない
  - 所要時間が表示される/されない条件を component テストで検証し green になっていること
  - _Requirements: 3.6_

- [x] 2.3 根拠の折りたたみ表示バリエーションを追加する
  - 表示バリエーション指定(展開 / 折りたたみ)を props で受け取り、省略時は現行どおり展開表示にする
  - 折りたたみ指定時は根拠を初期閉の折りたたみに格納し、展開操作(キーボード含む)で表示する
  - 初期閉・展開後表示を component テストで検証し green になっていること
  - _Requirements: 6.1, 6.2_

- [x] 3. ドリル確認画面を 2 カラムに再構成する(App.css のタイムライン状態クラスを 2.1 が先に追加するため、タスク 2 完了後に直列実行)
- [x] 3.1 2 カラムレイアウトと主カラムの構成
  - DOM 上サイド(共有 URL → ドリルの状態 → 設問別採点状況)を主内容より先に置き、広幅では grid-template-areas で main 左・side 右、狭幅では 1 カラムでサイドが先に表示される
  - 主カラム内の順序を「分析タイムライン → 設問一覧 → 回答一覧」とする
  - 共有 URL がスクロール前に視認できること、サイドが DOM 上で設問一覧より先にあること、主カラム先頭にタイムラインがあることをテストで検証
  - _Requirements: 1.1, 1.2, 1.3, 7.3_
  - _Boundary: DrillAdminPage_

- [x] 3.2 状態情報のサイド集約と重複排除
  - stat 行を廃止し、状態 chip・資料バージョン・回答数・採点済み数・平均点をサイドの状態カードに集約する。同じ値を複数箇所に表示しない
  - 分析の実行可否はボタンの活性状態のみで表現する(独立チップなし)
  - stat 行のラベル(4 分割の統計表示)が存在せず、回答数・平均点が単一箇所にのみ表示されることをテストで検証
  - _Requirements: 1.4, 1.5, 1.6_

- [x] 3.3 設問詳細の折りたたみ
  - 設問 ID・設問文・出題意図のみ常時表示し、模範解答・教材の根拠・ルーブリックは初期閉の折りたたみへ
  - ネイティブ details/summary によりキーボード開閉が機能する
  - 初期非表示と展開後表示をテストで検証し green になっていること
  - _Requirements: 2.1, 2.2, 2.3, 2.4_

- [x] 3.4 空状態案内の統合
  - 回答 0 件時の案内を共有 URL カード内のヒント 1 行に集約し、採点状況・回答一覧の空バナーを削除する
  - ドリル生成失敗時のエラーバナーは既存のまま表示される
  - 回答 0 件時に案内が 1 箇所のみ表示されることをテストで検証
  - _Requirements: 1.7, 1.8_

- [x] 3.5 分析実行中のフォーカス制御
  - 分析開始で設問一覧・回答一覧の折りたたみを閉状態に切り替える(1 秒ポーリングは変更しない)
  - 分析中も折りたたみを手動で再展開できる。分析失敗時は自動で開状態に復元し、成功時の画面遷移は現状のまま
  - 閉→再展開、失敗時の自動復元をテストで検証し green になっていること
  - _Requirements: 4.1, 4.2, 4.3, 7.4_
  - _Depends: 3.3_

- [x] 4. パッチレビュー画面の見出しと根拠を整理する
- [x] 4.1 (P) diff 見出しの変更規模とバージョン表記
  - diff 見出しに追加/削除行数を表示する(集計はタスク 1 の純粋関数を使用)
  - パッチ取得成功で即ページを表示し、講座情報は独立 state で後追い取得する。proposed かつ取得成功時のみ「v{n} → v{n+1}」を表示し、未解決・失敗時は表記を省略して diff とレビュー操作を継続する
  - 講座取得が未解決でも diff と適用・却下ボタンが表示されることをテストで検証
  - _Requirements: 5.1, 5.2, 5.3_
  - _Boundary: PatchReviewPage, DiffViewer_
  - _Depends: 1_

- [x] 4.2 根拠の一元化と表記整理
  - タイムラインを折りたたみ表示バリエーションで使用し、summary 中心の判断ログにする
  - FailureSignal カードを根拠の正として維持し、リスクノートは根拠カラム末尾に統合(独立カード廃止、空配列時は省略)
  - 生の drillRunId を表示せず、回答サンプル数は維持する。検証チェック一覧は追加しない
  - riskNotes の統合表示・生 ID 非表示・根拠の折りたたみを展開すると evidence が表示されることをテストで検証し green になっていること
  - _Requirements: 5.4, 6.1, 6.2, 6.3, 6.4, 6.5, 6.6_
  - _Depends: 2.3_

- [x] 5. 回帰確認と境界維持の検証
- [x] 5.1 e2e テストの更新
  - 「リスクノート」可視性 assertion を統合後の表示に更新し、設問詳細の展開が必要になった操作があれば summary クリックに更新する
  - learner フロー(回答・無効 token)の e2e が変更なしで green のまま
  - e2e の対象シナリオが green になっていること
  - _Requirements: 6.4, 7.1_
  - _Depends: 3.5, 4.2_

- [x] 5.2 境界維持の最終確認
  - typecheck / lint / unit / build を実行し green にする
  - learner 画面に rubric・idealAnswer・タイムラインが出ないこと、API 呼び出しが既存メソッドのみであること、ポーリング頻度が 1 秒のままであること、アクセシブルネームが維持されていることを確認する
  - モバイル幅で 1 カラム折り返しとサイド優先表示が崩れないことを確認する
  - frontend の typecheck / lint / test / build がすべて成功していること
  - _Requirements: 1.2, 7.1, 7.2, 7.3, 7.4, 7.5_

## Implementation Notes

- 2026-07-09: `frontend/src/lib/diffStats.ts` を追加し、`AnalysisTimeline` / `DiffViewer` / `DrillAdminPage` / `PatchReviewPage` と対応テスト・e2e を更新した。`npm run typecheck`、`npm run lint`、`npm test`、`npm run build`、`E2E_BACKEND_PORT=8013 E2E_FRONTEND_PORT=5176 npm run test:e2e` は成功。sandbox 内の初回 e2e は `uv` cache 権限で失敗したため、同一コマンドを許可済み昇格で実行した。
